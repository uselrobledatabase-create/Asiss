--- src/features/asistencia/components/dashboard/useDashboardTurnos.ts
+++ src/features/asistencia/components/dashboard/useDashboardTurnos.ts
@@ -15,4 +15,6 @@
     determineDailyShift,
     isPastDate,
+    getTurnoFromHorario,
+    isDateInRange,
 } from '../../../asistencia2026/utils/shiftEngine';
 import { TerminalCode } from '../../../../shared/types/terminal';
@@ -63,4 +65,4 @@
         return activeStaff.map((staff): ProcessedStaff => {
-            const hasLicense = licenses.some(l => l.staff_id === staff.id);
-            const hasVacation = vacations.some(v => v.staff_id === staff.id);
+            const hasLicense = licenses.some(l => l.staff_id === staff.id && isDateInRange(date, l.start_date, l.end_date));
+            const hasVacation = vacations.some(v => v.staff_id === staff.id && isDateInRange(date, v.start_date, v.end_date));
             const mark = marks.find(m => m.staff_id === staff.id);
@@ -83,9 +85,2 @@
 
-            const assignedTurno = determineDailyShift(
-                staff.horario,
-                staff.shift,
-                date,
-                templates,
-                staff.id,
-                shiftTypes
-            );
+            const assignedTurno = getTurnoFromHorario(staff.horario);
 
